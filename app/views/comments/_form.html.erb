<%= form_for [commentable, Comment.new] do |f| %>
  <div class="mb-6">
    <%= f.text_area :body, class: 'input input-with-border', placeholder: 'Add a component', required: true %>
  </div>

  <%= f.submit class: 'btn btn-default' %>
<% end %>
